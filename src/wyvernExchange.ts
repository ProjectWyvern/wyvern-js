import { Web3Wrapper } from '@0xproject/web3-wrapper';
import { Web3Provider, WyvernExchangeConfig } from './types';

export class WyvernExchange {
    constructor(provider: Web3Provider, config: WyvernExchangeConfig) {
    }
}
